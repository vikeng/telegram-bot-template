[project]
name = "tg-template"
version = "0.2.0"
description = ""
authors = [
    {name = "vikeng",email = "vikengui@gmail.com"}
]
requires-python = "^3.13"
dependencies = [
    "dotenv (==0.9.9)",
    "telebot (==0.0.5)",
    "flask (==3.1.3)",
    "waitress (==3.0.2)",
    "pytelegrambotapi (>=4.34.0,<5.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
